spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ashishjangde/microservice-config-repo
          username: ${GIT_USERNAME:ashishjangde}
          password: ${GIT_PASSWORD}
          default-label: main

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
